services:
  mobile:
    image: bank-sampah-mobile:latest
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://admin.sankara.cloud/api}
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://admin.sankara.cloud/api}
    ports:
      - "127.0.0.1:${APP_PORT:-3000}:3000"
